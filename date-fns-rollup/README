This subproject is for creating rollup of DateFns library to be used in enhanced-date-time-picker comonent for Vaadin 14.
It creates one file with needed functionality of library DateFns and packs it in the way that is importable by Vaadin 14 and also compatible with IE 11.

Modules that will be exported to file are definned in `index.js`

## Usage
First you need to install dependencies by running `npm install`
then run command: `npm run build`
It will create rollup with limited functionality of DateFns library and also minified version of rollup.
Result files can be found in `dist` folder.

once you made new rollup you should copy it to `enhanced-date-time-picker/enhanced-date-time-picker/src/main/resources/META-INF/resources/frontend/`